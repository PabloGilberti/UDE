{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build-arboles",
            "command": "g++",
            "args": [
                "-g",
                "${fileDirname}\\main.cpp",
                "${fileDirname}\\Arbol.cpp",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compilación manual usando g++ del sistema"
        }
    ]
}